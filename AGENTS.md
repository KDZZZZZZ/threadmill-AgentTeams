# Threadmill AgentTeams 协作规则

本文件适用于仓库根目录及其所有子目录。`third_party/agentteams/` 是归档的 AgentTeams 第三方代码；除非任务明确要求，不要把第三方代码的规则、发布流程或目录结构当作根项目规则。

## 分支管理

- `main` 和 `dev` 是受保护分支。
- 禁止直接向 `main` 或 `dev` 推送提交，包括强制推送、删除分支和绕过审查的自动化推送。
- 禁止直接在 `main` 或 `dev` 上合并本地分支。所有变更必须从独立工作分支提交 Pull Request（PR）后合入。
- 工作分支应从最新目标分支创建，建议使用 `agent/`、`feature/`、`fix/` 或 `docs/` 前缀；分支完成后及时同步目标分支并解决冲突。
- PR 合入前必须通过仓库要求的检查，并完成至少一名独立审查者的审查；未解决的审查意见不得合入。
- 不得使用 force-push 覆盖共享分支历史。工作分支需要改写历史时，应确认不会破坏他人协作，再使用安全的 `--force-with-lease`。

## PR 语言和内容

PR 标题、正文、审查回复和合入说明统一使用中文。PR 正文必须使用真实 Markdown，并至少包含以下小节：

```markdown
## 用户指令概要
- 用自己的话准确概括用户要求、边界和验收目标。

## 改动内容
- 列出实际修改的文件、模块和行为。

## 影响面
- 说明受影响的运行路径、目录、构建/部署流程、兼容性和用户可见变化。
- 明确说明没有影响的范围；不能用“无影响”替代分析。

## 验证结果
- 列出运行过的测试、检查命令和结果。
- 未运行的验证必须说明原因。

## 风险与回滚
- 说明已知风险、前置条件和回滚方式。

## Agent 自主设计与假设
- 逐项列出用户没有明确要求、由 Agent 自行决定的命名、目录、接口、策略或取舍，并说明理由。
- 如果没有，明确写“无”。不得把 Agent 的推断写成用户原始要求。
```

PR 中必须区分三类信息：用户明确提出的要求、仓库已有事实、Agent 为推进工作作出的设计或假设。需求存在实质歧义时，应在 PR 中记录假设及其影响；如果假设会改变范围、数据或外部系统状态，应先取得用户确认。

## Agent 执行要求

- 开始修改前检查当前分支、工作区状态和目标分支的最新状态；不得静默覆盖无关改动。
- 变更应保持小而可审查。移动、重命名和批量导入必须验证文件清单、内容完整性和路径引用。
- 完成前运行与改动匹配的最小验证，并在 PR 中记录证据；不能用未运行的测试结果声称验证通过。
- 根项目与第三方代码的构建、测试和发布命令必须分别从各自目录确认，避免因为相对路径变化误报成功。
- 不得提交密钥、凭据、临时缓存、构建产物或本地环境文件。
